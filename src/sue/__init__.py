"""SUE — прототип интеграции данных формата 1С:Розница в СУЭ.

Здесь находится единственное объявление версии: `pyproject.toml` читает её отсюда
(`tool.setuptools.dynamic`), а `GET /api/version` отдаёт то же значение. Так версия
совпадает при любом способе запуска — из исходников, из wheel или в контейнере.
"""

__version__ = "1.6.1"

__all__ = ["__version__"]
